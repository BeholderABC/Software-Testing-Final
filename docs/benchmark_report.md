# Benchmark Report

## Abstract

This report records reproducible performance and cost measurements for the AutoTestDesign pipeline. The deterministic rule path is measured for per-stage latency, and the language-model-backed stages are measured for token usage and cost when an API key is configured. Every figure cited in [test_plan.md](test_plan.md) and [cost_estimation.md](cost_estimation.md) traces back to this document. The report is regenerated by [scripts/benchmark.py](../scripts/benchmark.py).

---

## Provenance

- Generated at: 2026-05-29 17:03:21
- Host: Python 3.11.14 on Darwin arm64
- Repetitions per stage: 20
- Requirements measured: 12; test cases produced: 61.

---

## 1. Performance Requirement Verdict

The assignment targets test-case generation in under two seconds. The verdict is computed against the deterministic rule path (parse → structure → risk → coverage → test-case generation → oracle), which is network-independent and reproducible. The measured end-to-end generation latency on the rule path is 1.0 ms for 12 requirements yielding 61 cases. The two-second target is therefore **MET**.

The language-model-backed parse and risk stages reported in §3 incur network latency and are not counted against the generation budget; they execute once per session and fall back to the rule path when unavailable.

---

## 2. Rule-Pipeline Stage Latency

Per-stage latency is recorded in Table 1 and is averaged over 20 repetitions. The rule pipeline is deterministic, so latencies are stable across runs.

**Table 1.** Rule-pipeline stage latency.

| Stage | Mean (ms) | Min (ms) | Max (ms) | Stdev (ms) |
|---|---:|---:|---:|---:|
| parse (rule) | 0.221 | 0.125 | 1.613 | 0.321 |
| structure (rule) | 0.018 | 0.016 | 0.024 | 0.002 |
| risk (rule) | 0.479 | 0.417 | 0.691 | 0.07 |
| coverage | 0.005 | 0.004 | 0.011 | 0.002 |
| test-case generation | 0.136 | 0.126 | 0.255 | 0.028 |
| oracle attach | 0.113 | 0.109 | 0.133 | 0.007 |
| optimise (minimise) | 0.067 | 0.062 | 0.095 | 0.007 |
| export (CSV/JSON/Excel) | 19.607 | 15.785 | 71.786 | 12.18 |

---

## 3. LLM Token Usage and Cost

The model invoked is `qwen3.6-flash` with `temperature=0`. Token counts are read directly from the provider's `usage` field and are therefore exact. Per-call counts are recorded in Table 2.

**Table 2.** LLM call counts, latency, and token usage per session.

| Call | Count | Latency (ms) | Input tokens | Output tokens |
|---|---:|---:|---:|---:|
| Requirement parse (batch) | 1 | 23078.4 | 2577 | 2996 |
| Risk analysis (per requirement) | 12 | 14727.2 (mean) | 9616 | 21964 |
| **Total per session** | | | **12193** | **24960** |

### 3.1 Cost per Full Pipeline Run

At list price (¥1.2 per 1 M input tokens, ¥7.2 per 1 M output tokens), the session cost is recorded in Table 3.

**Table 3.** Cost per session.

| Pricing mode | Cost (CNY) |
|---|---:|
| Standard | ¥0.1943 |
| Batch (half price) | ¥0.0972 |

A complete run consumes 37153 tokens, costing approximately ¥0.1943 at list price. Even after dozens of interactive iterations the cumulative LLM cost is immaterial in comparison with labour cost.

---

## 4. Notes

The coverage, test-case-generation, oracle-synthesis, and optimisation stages issue no language-model calls and contribute zero token cost; they are deterministic rule engines. Latency depends on the host machine, so this report is regenerated on the demonstration host for presentation-accurate figures. Temporary export artefacts written during benchmarking are persisted under [outputs/_bench_tmp/](../outputs/_bench_tmp/) and may be deleted after each run.
